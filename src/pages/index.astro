---
import Layout from "$layouts/Layout.astro"
import { fmtDate } from "$lib/dates"

const postsData = await Astro.glob<{ title: string; date: string }>(
  "./20[0-9][0-9]/**/*.md",
)

const posts = postsData.map((x) => ({
  url: x.url,
  title: x.frontmatter.title,
  date: x.frontmatter.date,
}))
---

<Layout title="Happy Collision">
  <div class="home">
    <p>
      In truth, I am not a blogger. I help people create things. But below are
      some things I wrote. If you are interested in starting a project together,
      hit me up via my contact information at the bottom of the page.
    </p>

    <ul class="post-list mt-4">
      {
        posts.reverse().map(({ date, url, title }) => (
          <li>
            <h2>
              <a class="text-2xl link !no-underline" href={url}>
                {title}
              </a>
            </h2>
            <span class="post-meta">{fmtDate(date)}</span>
          </li>
        ))
      }
    </ul>

    <p class="rss-subscribe">
      <a class="link" href="/feed.xml"> subscribe via RSS</a>
    </p>
  </div>
</Layout>

<style></style>
